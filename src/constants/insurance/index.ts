export enum InsuranceRateType {
  Percentage = 'percentage',
  Fixed = 'fixed',
}

export enum InsuranceDeductibleRuleType {
  GreaterOf = 'greater_of',
  LesserOf = 'lesser_of',
  FixedOnly = 'fixed_only',
  PercentageOnly = 'percentage_only',
}
