export enum AuditSeverity {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}

// Enum for status types
export enum AuditStatus {
  SUCCESS = 'success',
  FAILURE = 'failure',
}
